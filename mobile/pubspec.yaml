name: mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.8 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Ícones padrão
  cupertino_icons: ^1.0.8
  
  # Nossas ferramentas de trabalho:
  image_picker: ^1.0.7       # Para tirar fotos
  flutter_tflite: ^1.0.1     # Para rodar a IA (adicionei esta linha que faltava)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # AQUI ESTÃO OS ARQUIVOS DA IA
  assets:
    - assets/modelo_soja.tflite
    - assets/labels.txt